package Learn.Collection;
/*
 * Collections：操作 Collection 和 Map 的工具类
 * - reverse(List list); 反转 list 中元素的顺序
 * - shuffle(List list); 对 list 集合元素进行随机排序
 * - sort(List list); 根据元素的自然顺序对指定 list 集合进行升序排序
 * - sort(List， Comparator);
 * - swap(List, int, int);
 *
 * Object max(Collection);
 * Object max(Collection, Comparator);
 * Object min(Collection);
 * Object min(Collection, Comparator);
 * int frequency(Collection, Object); 出现次数
 * void copy(List dest, List src); src ——> dest
 * boolean replaceAll(List list, Object oldVal, Object newVal);
 */
public class CollectionsTest {
    public static void main(String[] args) {

    }
}
